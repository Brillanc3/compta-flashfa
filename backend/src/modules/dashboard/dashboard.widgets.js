// /backend/src/modules/dashboard/dashboard.widgets.js

const widgets = [
    // Ce fichier est destiné à contenir les définitions des widgets "globaux"
    // qui ne sont liés à aucun module métier spécifique (ex: Horloge, Météo, Calendrier...).
    // Pour l'instant, il est vide.
];

module.exports = widgets;