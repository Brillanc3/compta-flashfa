// /backend/src/modules/ingest/ingest.permissions.js

/**
 * @fileoverview Fichier de permissions pour le module Ingest.
 *
 * NOTE : Ce module utilise une authentification par clé d'API directement dans le contrôleur
 * plutôt que le système de permissions basé sur les utilisateurs.
 * Par conséquent, ce fichier ne définit aucune permission spécifique.
 */

const PERMISSIONS = {};

const HIERARCHY = {};

module.exports = {
    PERMISSIONS,
    HIERARCHY,
};