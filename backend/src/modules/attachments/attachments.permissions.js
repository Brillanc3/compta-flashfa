// backend/src/modules/attachments/attachments.permissions.js
'use strict';

const PERMISSIONS = {
    // Public GET pour l’instant (visionnage via publicId).
    // Permissions futures possibles si tu veux restreindre (ex: VIEW_ATTACHMENTS).
};

const HIERARCHY = {};

module.exports = { PERMISSIONS, HIERARCHY };